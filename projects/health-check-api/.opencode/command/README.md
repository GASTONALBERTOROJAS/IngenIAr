# Project-local OpenCode commands

Use this folder for optional custom OpenCode commands that are specific to the generated project.

Examples:
- project bootstrap helpers
- domain-specific QA shortcuts
- release or validation commands
